
📋 项目面试问题记录
项目名称：船舶数字化管理系统（NEXMAR）

═══════════════════════════════════════════════════════
一、项目概述与架构
═══════════════════════════════════════════════════════

Q1：请简单介绍一下你的项目，解决了什么问题？
考察点：项目理解能力、表达能力

标准回答：
这个项目叫做"船舶数字化管理系统"，是我独立开发的一个全栈项目。它的核心目标是将船舶公司日常管理的船舶信息、船员档案、航次记录、人员任职分配等业务流程从纸质或 Excel 管理迁移到线上系统，实现数据的统一管理和快速查询。

系统分为五个核心模块：船舶信息管理（包括船名、IMO 编号、船型、尺寸、主机型号等 20 多个字段）、船员档案管理（含证书信息的 JSON 存储）、航次记录管理（出发港、到达港、预计/实际时间等）、任职分配管理（船员在船上的岗位、薪资、上下船时间），以及用户管理（支持百万级用户数据的分页查询）。

技术上，前端使用 Vue 3 + Vite 构建，后端使用 Spring Boot 3.5 + MyBatis-Plus + MySQL 8.0，数据库连接池使用阿里 Druid。项目已经部署在阿里云轻量服务器上，通过 Nginx 反向代理对外提供服务。
前端发起请求，程序需要查询数据库封装分页 Page 对象；
程序向连接池申请一条数据库连接；
池子分配一条空闲连接（电话线）；
通过这条连接发送 SQL，数据库返回数据；
程序把返回数据封装成 Page、List 实体返回前端；
操作完成，不是关闭数据库连接，而是把连接还给连接池；
下一个请求可以重复使用这条连接。

外网用户（公网）
↓（公网通道，所有人可访问）
Nginx（暴露公网，唯一对外入口）
↓（内网通道，仅服务器 / 局域网内部互通，外网进不来）
后端业务项目（SpringBoot/Java 等）
外网连接谁？
外网只连接 Nginx，碰不到后端项目。
Nginx 连接谁？
Nginx 连接内网里的后端项目（本机 127.0.0.1 或局域网 192.168 段）。
───────────────────────────────────────────────────────

Q2：你的项目采用了什么架构？前后端是如何分离的？
考察点：对前后端分离架构的理解

标准回答：
项目采用前后端分离架构。前端是一个独立的 Vue 3 项目，使用 Vite 作为构建工具，开发时监听 5173 端口；后端是一个独立的 Spring Boot 项目，监听 2005 端口，所有 API 路径统一以 /api 作为 context-path。

前后端的通信方式是 RESTful API，前端通过原生 fetch 发送 HTTP 请求到 /api 开头的路径。在开发环境下，Vite 的 devServer proxy 配置会将 /api 请求代理到 http://localhost:2005，解决跨域问题。在生产环境下，通过 Nginx 反向代理实现同样的效果，Nginx 监听 80 端口，将 /api/ 路径的请求 proxy_pass 到 http://127.0.0.1:2005/api/。

这样前端和后端可以独立开发、独立部署、独立扩展，互不影响。

Vue3 是不是前端框架？
是，Vue3 标准前端开发框架，专门写页面、交互、界面。
作用：写网页按钮、表格、登录页、展示数据，对接后端接口。

SpringBoot 自带内嵌 Tomcat，可以直接启动后端服务；
Vite 就是前端配套的启动 + 打包工具，专门给 Vue/JS 项目用。

5173 和后端 localhost:2005 是不是一回事？
逻辑一样，都是本机端口，只是用途不同：
localhost:5173：Vite 启动的前端页面服务，打开网页看界面；
localhost:2005：SpringBoot 后端服务，提供接口；
开发时，页面跑 5173，接口跑 2005，前后端两个独立程序分开运行，这就是前后端分离。

/api：后端接口统一前缀，用来区分页面和接口，方便 Nginx 分流转发。
区分页面和接口
前端页面路径没有 /api，业务接口全部带 /api；搭配 Nginx 反向代理很好区分转发规则：
访问 /、/index → 走前端静态页面
访问 /api/ 开头 → 转发给后端 2005 端口
避免路径冲突
假如前端有个页面路径叫 /login，后端接口也叫 /login，不加 /api 会地址冲突，分不清该给谁。
方便统一配置代理
前端开发时配置代理，所有 /api 请求自动转发到后端 2005，不用每次写完整后端地址。

整条流程梳理，一次性理顺
电脑装 Node → 自带 npm 工具；
创建 Vue 项目时，npm 自动下载 Vue3、Vite 存到 node_modules；
本地开发：npm run dev，调用已安装的 Vite 启动 5173 前端页面；
开发完成打包：npm run build，复用本地已装好的 Vite 编译代码，生成 dist；
dist 上传服务器，Nginx 托管页面；后端 SpringBoot 内嵌 Tomcat 跑 2005 端口接口；
Nginx 分流：页面走 dist 静态资源，/api 路径转发给 SpringBoot 后端。

───────────────────────────────────────────────────────

Q3：为什么选择前后端分离架构？它有什么优缺点？
考察点：技术选型的思考深度

标准回答：
选择前后端分离主要有几个考虑：

优点：
第一，职责分离。前端专注页面交互和用户体验，后端专注业务逻辑和数据处理，开发效率高，团队协作更清晰。
第二，独立部署。前端打包成静态文件放到 Nginx，后端打包成 JAR 运行，互不影响。前端更新不需要重启后端，后端更新也不影响前端资源。
第三，多端适配。后端提供统一的 RESTful API，未来如果要开发移动端 App 或小程序，可以直接复用同一套接口。
第四，技术栈独立。前端可以自由选择 Vue、React 等框架，后端可以独立升级 Spring Boot 版本。

缺点：
第一，跨域问题。前后端不同源，需要额外处理 CORS 或配置反向代理。
第二，部署复杂度增加。需要分别管理前端 Nginx 和后端 Java 进程，运维成本比单体应用稍高。
第三，SEO 不友好。纯前端渲染的页面对搜索引擎爬虫不友好（不过管理系统不需要 SEO，所以这点不影响）。

对于船舶管理系统这类后台管理应用，前后端分离是非常合适的选择。


═══════════════════════════════════════════════════════
二、技术栈相关
═══════════════════════════════════════════════════════

Q4：你使用了哪些核心技术？为什么选择 MyBatis-Plus 而不是 MyBatis 或 JPA？
考察点：技术选型能力

标准回答：
核心技术栈：前端 Vue 3.5 + Vite 8，后端 Spring Boot 3.5 + MyBatis-Plus 3.5.9 + MySQL 8.0 + Druid 1.2.28 连接池，使用 Lombok 简化实体类代码，DataFaker 生成测试数据。

选择 MyBatis-Plus 的原因：
相比原生 MyBatis，MyBatis-Plus 提供了通用的 CRUD 方法（insert、deleteById、updateById、selectById 等），不需要手写 XML Mapper 文件，开发效率提升很多。它还内置了条件构造器 LambdaQueryWrapper，可以用 Java Lambda 表达式构建查询条件，类型安全且代码可读性好。分页方面，MyBatis-Plus 的分页插件只需要注册一个 PaginationInnerInterceptor，调用 page() 方法就能自动分页，不需要手写 LIMIT 语句。

相比 JPA/Hibernate，MyBatis-Plus 对 SQL 的控制力更强。JPA 的 HQL 在处理复杂查询时不够灵活，而 MyBatis-Plus 既能在简单场景下用内置方法快速开发，也能在复杂场景下自定义 SQL。另外 MyBatis-Plus 对国内开发者更友好，中文文档完善，社区活跃。

───────────────────────────────────────────────────────

Q5：Spring Boot 3.5 和 Spring Boot 2.x 有什么区别？为什么选择 Java 17？
考察点：对框架版本演进的关注度

标准回答：
Spring Boot 3.x 相比 2.x 有几个重大变化：
第一，最低要求 Java 17。Spring Boot 3 基于 Spring Framework 6，而 Spring 6 要求 Jakarta EE 9+，对应的命名空间从 javax.* 迁移到了 jakarta.*。
第二，原生支持 GraalVM 原生镜像编译，启动速度和内存占用有显著优化。
第三，对 Spring Security 进行了大幅重构，API 更简洁。
第四，废弃了很多旧的自动配置类，改用新的命名空间。

选择 Java 17 的原因：Java 17 是 LTS（长期支持）版本，Oracle 提供长期维护。相比 Java 8，Java 17 引入了 record 类、sealed 类、pattern matching、text block、switch 表达式等现代语法特性，代码更简洁。同时 Java 17 的 GC（G1 垃圾回收器）性能比 Java 8 的 Parallel GC 好很多，适合服务端应用。

───────────────────────────────────────────────────────

Q6：你的连接池为什么选择 Druid？它相比 HikariCP 有什么优势？
考察点：对中间件的了解深度

标准回答：
选择 Druid 主要看中它的运维和监控能力。

Druid 相比 HikariCP 的优势：
第一，监控面板。Druid 内置了 StatFilter，可以统计每条 SQL 的执行时间、调用次数、慢 SQL 等，通过内置的 Web 页面就能直观看到数据库访问情况，对排查性能问题非常有帮助。
第二，SQL 防火墙。Druid 的 WallFilter 可以防止 SQL 注入，比如拦截 DELETE 不带 WHERE 条件的危险语句。
第三，扩展性好。Druid 提供了丰富的 Filter 链机制，可以方便地添加日志、统计、加密等自定义过滤器。

HikariCP 的优势在于性能，它是目前最快的连接池，字节码层面做了极致优化。Spring Boot 2.x 之后默认使用 HikariCP。但对于我的项目来说，Druid 的监控功能更有价值，性能差异在中小规模项目中几乎感受不到。

我的配置是：初始连接数 5、最小空闲连接 5、最大活跃连接 20、最大等待时间 60 秒。


═══════════════════════════════════════════════════════
三、数据库设计
═══════════════════════════════════════════════════════

Q7：请描述你的数据库表结构以及表之间的关系。
考察点：数据库设计能力

标准回答：
数据库使用 MySQL 8.0，InnoDB 引擎，utf8mb4 字符集。共有 5 张核心业务表：

第一张：ship_info（船舶信息表），主键 id 自增，包含船名、IMO 编号（唯一标识）、船籍港、船舶类型、建造年份、总长、型宽、型深、排水量、主机型号、航速、造船厂、经营公司、检验有效期、船舶状态等 20 多个字段，以及 createTime 和 updateTime 时间戳。

第二张：ship_crew_members（船员档案表），主键 crew_id 自增，存储船员固有信息：姓名、证书（JSON 格式）、职位、电话、紧急联系人和紧急联系电话。

第三张：ship_crew_assignments（任职履历表），主键 assignment_id 自增，这是一张多对多的中间表，通过 ship_id 外键关联 ship_info，通过 crew_id 外键关联 ship_crew_members，记录船员在每艘船上的岗位、薪资、上船时间和下船时间。

第四张：ship_voyages（航次记录表），主键 voyage_id 自增，通过 ship_id 外键关联船舶，记录航次编号、出发港、到达港、预计出发时间、实际到达时间、货物重量等。

第五张：user_info（用户表），主键 id 自增，包含用户名、密码、姓名、邮箱、电话、状态、登录失败次数、锁定时间、最后登录 IP、最后登录时间等，支持软删除（is_deleted 字段）。

表间关系：ship_info 和 ship_crew_members 是多对多关系，通过 ship_crew_assignments 中间表关联，中间表上建了两个外键约束。ship_voyages 和 ship_info 是多对一关系，多条航次记录属于同一艘船。

───────────────────────────────────────────────────────

Q8：ship_crew_assignments 表是怎么关联船舶和船员的？为什么需要这张中间表？
考察点：多对多关系的理解

标准回答：
ship_crew_assignments 表中有两个外键字段：ship_id 关联 ship_info 表的 id 主键，crew_id 关联 ship_crew_members 表的 crew_id 主键。数据库层面通过 CONSTRAINT 外键约束保证引用的完整性。

需要中间表的原因是船舶和船员之间是多对多关系：一艘船上会配备多名船员（船长、大副、二副、轮机长、水手、厨师等），而一个船员在其职业生涯中也可能在不同时间段任职于不同的船舶。这种多对多关系在关系型数据库中无法直接用两张表表示，必须通过第三张中间表来拆解成两个一对多关系。

中间表除了两个外键，还存储了关联本身的属性：job_position（在该船上的岗位）、salary（该段任职的薪资）、board_date（上船时间）、disembark_date（下船时间，NULL 表示当前在船）。这样就能完整记录一个船员在不同船舶上的任职履历。

───────────────────────────────────────────────────────

Q9：船员的证书信息（certificate）在数据库中是怎么存储的？为什么这样设计？
考察点：复杂数据类型的存储方案

标准回答：
船员的证书信息使用 JSON 格式存储在 ship_crew_members 表的 certificate 单个字段中。每个证书包含 certName（证书名称）和 certNo（证书编号）两个属性，一个船员可以有多本证书，所以是一个 JSON 数组。

在 Java 层面，实体类 Crew 中 certificate 字段的类型是 List<Certificate>，其中 Certificate 是一个静态内部类。字段上标注了 @TableField(typeHandler = JacksonTypeHandler.class)，MyBatis-Plus 在读取数据时会自动调用 Jackson 将 JSON 字符串反序列化为 List 对象，写入时自动序列化为 JSON 字符串。同时实体类的 @TableName 注解中配置了 autoResultMap = true，确保 MyBatis-Plus 自动生成的 ResultMap 能正确使用 TypeHandler。

选择这种设计的原因：证书是船员的附属信息，不需要独立查询（不会单独搜索某个证书），和船员是一对一的归属关系。如果拆成单独的证书表，查询船员信息时就需要额外的 JOIN 操作，增加了复杂度。JSON 存储在这种"主从一体、不需要独立检索"的场景下是最简洁高效的方案。

───────────────────────────────────────────────────────

Q10：你的 IMO 编号为什么存储为字符串而不是整数？
考察点：数据类型的选择

标准回答：
IMO（International Maritime Organization）编号是国际海事组织为每艘船舶分配的唯一标识符，由 7 位纯数字组成。虽然看起来是数字，但我选择用 VARCHAR 字符串存储，原因有三：

第一，IMO 编号本质上是一个标识符，不是数值。我们不会对它做加减乘除等数学运算，它的用途就是唯一标识一艘船，这和电话号码、身份证号一样，应该用字符串存储。

第二，避免前导零丢失的风险。如果用整数存储，在某些场景下（如 Excel 导入、前端展示）可能会出现前导零被截断的问题。虽然 IMO 编号规定第一位不为零，但用字符串存储更加安全，不会有意外情况。

第三，便于前端展示和校验。字符串类型可以直接在前端用正则表达式校验格式（7 位数字），不需要做数字到字符串的转换。


═══════════════════════════════════════════════════════
四、后端实现细节
═══════════════════════════════════════════════════════

Q11：你的项目分层是怎么做的？Controller、Service、Mapper 各层的职责是什么？
考察点：分层架构理解

标准回答：
项目采用经典的三层架构：

Controller 层（controller 包）：负责接收 HTTP 请求、参数校验、调用 Service 层、返回响应。每个 Controller 用 @RestController 注解标注，通过 @RequestMapping 定义路由前缀。比如 ShipInfoController 的路由前缀是 /shipInfo，提供 GET（查询）、POST（新增）、PUT（修改）、DELETE（删除）和 GET /list（分页查询）五个接口。Controller 层不包含任何业务逻辑，只做请求转发。

Service 层（service 包）：负责业务逻辑处理。当前项目的 Service 接口继承了 MyBatis-Plus 的 IService<T>，自动获得了 save、saveBatch、updateById、removeById、page、count 等通用方法。Service 实现类继承 ServiceImpl<Mapper, Entity>。如果将来有复杂业务（比如分配船员时校验该船员是否已在其他船上任职），业务逻辑就写在 Service 层。

Mapper 层（inter 包）：负责数据访问，继承 MyBatis-Plus 的 BaseMapper<T>。BaseMapper 提供了最基础的 SQL 操作方法。每个 Mapper 接口用 @Mapper 注解标注，Spring Boot 启动时会自动扫描并生成代理实现类。

这种分层的好处是职责清晰、便于维护和测试。如果将来需要修改数据库访问逻辑，只需要改 Mapper 层，不影响 Controller 和 Service。

───────────────────────────────────────────────────────

Q12：你的分页是怎么实现的？MyBatis-Plus 分页插件的原理是什么？
考察点：分页机制的理解深度

标准回答：
分页的实现分三步：

第一步，注册分页插件。在 MybatisPlusConfig 配置类中，通过 @Bean 方法创建 MybatisPlusInterceptor 对象，然后调用 addInnerInterceptor 方法添加 PaginationInnerInterceptor(DbType.MYSQL)。这个拦截器是针对 MySQL 数据库的分页拦截器。

第二步，Controller 层接收分页参数。前端传入 page（当前页码）和 count（每页条数）两个请求参数，后端用 @RequestParam 接收，并做了安全限制：如果 count 超过 100，就强制设为 100。

第三步，调用分页方法。通过 shipInfoService.page(new Page<>(page, count)) 执行分页查询，返回 IPage<ShipInfo> 对象，其中包含 records（当前页数据列表）、total（总记录数）、size（每页大小）、current（当前页码）等信息。

分页插件的原理：MyBatis-Plus 的 PaginationInnerInterceptor 是一个 SQL 拦截器。当执行分页查询时，拦截器会做两件事：① 先自动执行一条 SELECT COUNT(*) 查询获取总记录数；② 然后改写原始 SQL，在末尾追加 LIMIT 和 OFFSET 子句。比如原始 SQL 是 SELECT * FROM ship_info，会被改写成 SELECT * FROM ship_info LIMIT 10 OFFSET 0（第一页，每页 10 条）。这样开发者就不需要手写分页 SQL 了。

───────────────────────────────────────────────────────

Q13：为什么分页接口要限制 count 最大为 100？
考察点：性能意识、安全防护

标准回答：
这是一个防御性编程的措施，主要防止两种问题：

第一，数据库压力。如果前端传入 count=1000000，MyBatis-Plus 会生成 SELECT * FROM ship_info LIMIT 1000000 这样的 SQL，数据库需要一次性扫描和返回 100 万条记录，可能导致查询超时、CPU 飙升、甚至数据库连接被耗尽。

第二，内存溢出。100 万条记录从数据库传输到 Java 应用后，需要反序列化成 100 万个 Java 对象，每个 ShipInfo 对象有 20 多个字段，内存占用可能达到几百 MB 甚至 GB 级别，很容易触发 OutOfMemoryError。

第三，网络拥塞。大量数据从后端传输到前端，会占用大量网络带宽，影响其他用户的正常请求。

限制 count 最大 100 条，前端通过翻页来浏览全部数据，既保证了用户体验，又保护了系统稳定性。这是一个典型的"后端不能信任前端输入"的安全原则。

───────────────────────────────────────────────────────

Q14：你的路由为什么从 /page 改成了 /list？
考察点：RESTful 路由设计、Spring MVC 路由冲突

标准回答：
这是一个实际开发中遇到的路由冲突问题。

原来的分页接口路径是 /shipInfo/page，同时有一个根据 ID 查询的接口路径是 /shipInfo/{id}。问题在于 Spring MVC 的路径匹配机制：当请求 /shipInfo/page 时，Spring MVC 无法确定 "page" 是一个固定的路径字符串还是应该匹配 {id} 路径变量。在某些情况下，Spring MVC 会把 "page" 当作 id 的值传给 getbyId 方法，导致类型转换错误（String 转 Long 失败）。

解决方案就是把分页接口的路径从 /page 改成 /list。因为 /list 和 /{id} 不存在歧义——Spring MVC 在匹配时会优先匹配精确路径 /list，只有不匹配任何精确路径时才会走 /{id} 的通配。

这是 Spring MVC 的一个常见坑：路径变量 {id} 不要和固定路径放在同一层级，否则容易产生冲突。最佳实践是给非资源标识符的路径使用不同的前缀或层级。

───────────────────────────────────────────────────────

Q15：你的跨域是怎么处理的？WebConfig 中的配置各字段含义是什么？
考察点：跨域问题的理解

标准回答：
跨域问题是因为浏览器的同源策略：前端运行在 localhost:5173，后端运行在 localhost:2005，协议、域名或端口不同就是"跨域"，浏览器会阻止跨域请求。

我的处理方式有两种场景：

开发环境：通过 Vite 的 devServer proxy 配置，将所有 /api 开头的请求代理到 http://localhost:2005。这样浏览器认为请求的是同源的 Vite 服务器，由 Vite 服务端转发到后端，绕过了浏览器的同源策略。

生产环境：前端打包后放到 Nginx，后端 API 也通过 Nginx 反向代理，前端和 API 在同一个域名下，不存在跨域问题。

同时后端也配置了 CORS 作为兜底。WebConfig 类实现了 WebMvcConfigurer 接口，重写了 addCorsMappings 方法：
- addMapping("/**")：对所有路径生效
- allowedOriginPatterns("*")：允许所有来源域名
- allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS")：允许的 HTTP 方法
- allowedHeaders("*")：允许所有请求头
- allowCredentials(true)：允许携带 Cookie 等凭证

这样即使不走代理，后端也能正确处理跨域请求。

───────────────────────────────────────────────────────

Q16：登录接口的密码是明文存储的，你觉得这有什么安全隐患？如何改进？
考察点：安全意识

标准回答：
当前登录接口直接用 request 中的密码和数据库中的密码做 equals 比较，意味着数据库存储的是明文密码。这有严重的安全隐患：

第一，一旦数据库被拖库（比如 SQL 注入攻击、备份文件泄露），所有用户的密码直接暴露。很多用户会在多个网站使用相同密码，这意味着用户的其他账号也可能受到威胁。

第二，内部人员（如有数据库访问权限的运维人员）可以直接看到所有人的密码。

第三，不符合等保合规要求。

改进方案：使用 BCrypt 加密算法。BCrypt 是一种自带盐值的单向哈希算法，每次加密结果不同（因为随机盐值），即使数据库泄露，攻击者也无法反推出原始密码。

具体实现：
① 引入 Spring Security Crypto 模块或 BCrypt 工具类
② 用户注册时，调用 BCryptPasswordEncoder.encode(password) 生成哈希值存入数据库
③ 登录时，调用 BCryptPasswordEncoder.matches(rawPassword, encodedPassword) 比对
④ BCrypt 的计算成本可配置（默认 10 轮），故意设计得较慢（约 100ms），防止暴力破解

另外还可以加一层防护：在登录接口加验证码和登录失败次数限制，当前 user_info 表已经有 loginFailCount 和 lockTime 字段，说明已经考虑了账号锁定机制。


═══════════════════════════════════════════════════════
五、前端实现细节
═══════════════════════════════════════════════════════

Q17：你的前端为什么没有使用 Vue Router 和 Pinia？状态管理是怎么做的？
考察点：状态管理方案的选择

标准回答：
这是一个基于项目复杂度的权衡决策。

这个管理系统只有登录页和 Dashboard 两个页面，Dashboard 内部通过 Tab 切换五个面板组件。不需要多页面路由跳转，所以没必要引入 Vue Router。组件切换直接在 App.vue 中用 v-if/v-else 实现，配合 Transition 组件做过渡动画。

状态管理同样比较简单：
① 登录状态：App.vue 中用 isLoggedIn 响应式变量管理，初始值从 localStorage 读取（页面刷新后保持登录状态）。
② 用户信息：登录成功后，将用户对象 JSON.stringify 存入 localStorage（key 为 ship_user），退出时删除。
③ 组件通信：父组件 App.vue 通过 props 把用户信息传给 DashboardView，DashboardView 再通过 props 传给各面板组件。子组件通过 emit 向父组件发送事件（如退出登录）。
④ 各面板组件的数据（如船舶列表、分页状态）是组件内部的局部状态，用 ref 管理，不需要跨组件共享。

如果项目规模扩大，比如需要添加更多页面、需要全局状态共享（如购物车、消息通知），我会引入 Vue Router 做路由管理，Pinia 做状态管理。但目前的项目规模，localStorage + props/emit 已经完全够用，引入额外依赖反而增加了复杂度。

───────────────────────────────────────────────────────

Q18：App.vue 中 Transition 组件的 mode="out-in" 是什么意思？
考察点：Vue 过渡动画的理解

标准回答：
Transition 是 Vue 3 的内置过渡组件，用于在元素插入、移除或切换时应用进入/离开动画。

mode 属性控制过渡的执行顺序：
- mode="out-in"：先执行旧元素的离开动画，等离开动画完成后，再执行新元素的进入动画。效果是"先走后进"。
- mode="in-out"：先执行新元素的进入动画，等进入动画完成后，再执行旧元素的离开动画。效果是"先进后走"。
- 不设置 mode（默认）：新旧元素同时过渡，即离开的同时进入。

在我的项目中，App.vue 用 Transition 包裹了 LoginView 和 DashboardView 的切换，设置了 mode="out-in"。这样用户登录成功后，登录页面先淡出消失，然后 Dashboard 页面再淡入出现，过渡自然流畅。同样，DashboardView 内部的面板切换也使用了 Transition，切换 Tab 时旧面板先淡出，新面板再淡入。

CSS 动画通过 .fade-enter-active（进入时的过渡效果）、.fade-leave-active（离开时的过渡效果）、.fade-enter-from（进入起始状态 opacity: 0）和 .fade-leave-to（离开结束状态 opacity: 0）四个类实现 0.4 秒的透明度渐变效果。

───────────────────────────────────────────────────────

Q19：你的前端搜索过滤是在哪一层做的？有什么优缺点？
考察点：前后端职责划分

标准回答：
当前搜索过滤是在前端通过 Vue 的 computed 计算属性实现的。以船舶信息面板为例，fetchShips 方法从后端获取当前页的数据存入 ships 响应式数组，然后 filteredShips 计算属性根据 searchQuery 关键词对 ships 数组做本地过滤，匹配船名、IMO、船籍港、船舶类型四个字段。

优点：
第一，响应速度快。过滤是纯前端操作，不需要发送网络请求，用户输入关键词后结果即时更新，体验流畅。
第二，减轻服务器压力。过滤逻辑在浏览器端执行，不会给后端增加额外的查询请求。
第三，实现简单。利用 Vue 的 computed 自动追踪依赖，searchQuery 变化时自动重新计算，代码量少。

缺点：
第一，只能搜索当前页的数据。比如当前页显示 10 条记录，搜索范围就是这 10 条，其他页的数据即使包含关键词也不会出现在结果中。这在数据量大时会导致搜索结果不完整。
第二，每次翻页后需要重新过滤。因为翻页会重新请求后端数据，ships 数组更新后 filteredShips 会基于新数据重新过滤。

改进方案：如果需要全局搜索，应该把搜索逻辑移到后端，在 API 接口中增加 keyword 查询参数，后端通过 SQL 的 LIKE 或全文索引实现跨页搜索。

───────────────────────────────────────────────────────

Q20：Vite 的 base: './' 配置是做什么的？为什么需要它？
考察点：构建工具配置理解

标准回答：
base 配置项决定了打包后静态资源的公共路径前缀。

默认情况下，Vite 的 base 是 '/'，打包后 index.html 中引用的 JS/CSS 路径是绝对路径，比如 /assets/index-abc123.js。这意味着浏览器会从域名根路径去加载资源。

设置为 './' 后，打包后的资源路径变成相对路径，比如 ./assets/index-abc123.js。浏览器会基于当前 HTML 文件的位置去加载资源。

为什么需要它：我的项目部署在 Nginx 上，前端静态资源放在 /www/wwwroot/ship-front 目录下。如果使用绝对路径 '/'，Nginx 需要从根路径开始查找资源，可能找不到。使用相对路径 './' 后，无论 HTML 文件在哪个目录下，资源引用都是相对于 HTML 文件自身位置的，确保了资源能正确加载。

这是一个部署友好的配置，特别是在不确定最终部署路径、或者需要部署到子目录的场景下非常实用。


═══════════════════════════════════════════════════════
六、测试与数据
═══════════════════════════════════════════════════════

Q21：你的测试数据是怎么生成的？为什么使用 DataFaker？
考察点：测试数据管理

标准回答：
测试数据通过 Java 的 DataFaker 库在单元测试中生成。具体做法是写一个 @SpringBootTest 的测试类 UserDataTest，注入各 Service，然后用循环批量生成数据并调用 saveBatch 方法插入数据库。

DataFaker 是一个开源的假数据生成库，支持多种语言和地区。我使用 new Faker(new Locale("zh-CN")) 创建了一个中文 Faker 实例，它可以生成中文姓名、中文邮箱、IPv4 地址等真实感很强的测试数据。

对于用户数据，我额外准备了 200 个常见中文姓氏和 500 个常见中文名字的数组，通过随机组合生成更自然的中文姓名。手机号也是通过真实的手机号段前缀（如 130、131、150 等）加上随机 8 位数字生成的。

用户表生成了 100 万条数据，目的是验证分页查询在大数据量下的性能表现。测试结果显示，即使有百万用户，分页查询（每页 10 条）的响应时间也在毫秒级别。

───────────────────────────────────────────────────────

Q22：批量插入数据时为什么要每 1000 条提交一次？
考察点：批量操作的性能优化

标准回答：
这是一个批量操作的内存和事务管理策略。

如果一次性把 100 万条数据全部放进 List 然后调用一次 saveBatch，会有以下问题：
第一，内存溢出。100 万个 Java 对象同时存在于内存中，每个 User 对象有十几个字段，加上 Java 对象头、引用等开销，内存占用可能超过 1GB，容易触发 OutOfMemoryError。
第二，事务过大。MyBatis-Plus 的 saveBatch 默认在一个事务中执行，100 万条 INSERT 语句在一个事务里，数据库的 undo log 和 redo log 会非常大，可能导致事务超时或数据库锁等待。
第三，网络传输。一次性发送大量数据，可能超过 MySQL 的 max_allowed_packet 限制。

每 1000 条提交一次的好处：
① 内存可控。每次只维护 1000 个对象的 List，提交后清空，内存占用始终很低。
② 事务粒度小。每个批次是一个独立事务，即使某一批失败，不影响之前已提交的数据。
③ 可以打印进度。每处理 10 万条打印一次进度，方便监控执行情况。

这是实际开发中处理大批量数据的通用做法，核心原则是"分批处理、控制内存"。


═══════════════════════════════════════════════════════
七、性能与安全
═══════════════════════════════════════════════════════

Q23：如果用户量增长到百万级，你的系统瓶颈会在哪里？如何优化？
考察点：性能分析能力

标准回答：
实际上我的 user_info 表已经生成了 100 万条数据做了验证。基于测试结果和项目架构，瓶颈分析和优化方案如下：

数据库层（最可能的瓶颈）：
瓶颈：百万级数据的分页查询，尤其是深页翻页（如第 10000 页），MySQL 需要扫描大量数据再丢弃，查询会变慢。多表 JOIN 查询（如查询某艘船的所有船员）在数据量大时也会变慢。
优化：① 对常用查询字段加索引（如 ship_info 的 imo 字段、user_info 的 username 字段）；② 深页翻页优化用"游标分页"代替 OFFSET，即 WHERE id > last_id LIMIT 10；③ 读写分离，主库写、从库读；④ 数据量继续增长可以考虑分库分表。

后端层：
瓶颈：每个请求都要查数据库，没有缓存，高并发时数据库连接池（最大 20 个连接）可能不够用。
优化：① 引入 Redis 缓存热点数据（如船舶列表、用户信息）；② 调大 Druid 连接池的 max-active；③ 对耗时操作使用异步处理。

前端层：
瓶颈：大数据量的表格渲染可能卡顿。
优化：① 使用虚拟滚动（只渲染可视区域的 DOM 节点）；② 组件懒加载；③ 静态资源加 CDN 加速。

───────────────────────────────────────────────────────

Q24：你的接口没有做任何鉴权，如果部署到生产环境，需要做哪些安全措施？
考察点：安全意识

标准回答：
当前所有接口都是开放的，任何人知道 URL 就能访问和操作数据，这在生产环境是不可接受的。需要做的安全措施：

认证层（你是谁）：
① 引入 JWT（JSON Web Token）认证。用户登录成功后，后端生成一个 JWT Token 返回给前端，前端每次请求在 Header 中携带 Token（Authorization: Bearer xxx）。后端通过过滤器校验 Token 的合法性。
② 密码加密存储，使用 BCrypt 替代明文。

授权层（能做什么）：
① 引入 Spring Security 或自定义权限拦截器，基于角色（RBAC）控制接口访问权限。比如普通用户只能查看，管理员才能增删改。
② 接口级别的权限注解，如 @PreAuthorize("hasRole('ADMIN')")。

防护层：
① 接口限流。使用 Guava RateLimiter 或 Redis + Lua 脚本限制每个 IP 的请求频率，防止恶意刷接口。
② HTTPS。生产环境已配置 SSL，保证数据传输加密。
③ SQL 注入防护。MyBatis-Plus 使用参数化查询，天然防止 SQL 注入，但需要确保没有手写拼接 SQL 的地方。
④ XSS 防护。前端对用户输入做转义，后端可以加 XSS 过滤器。
⑤ 敏感信息脱敏。手机号、邮箱等返回前端时做部分遮掩（如 138****1234）。

审计层：
① 操作日志。记录谁在什么时间做了什么操作，方便追溯。
② 登录日志。记录登录 IP、时间、失败次数，异常登录告警。

───────────────────────────────────────────────────────

Q25：Druid 连接池的 initial-size: 5、min-idle: 5、max-active: 20 分别是什么意思？
考察点：连接池配置理解

标准回答：
这三个参数控制连接池的生命周期行为：

initial-size: 5 —— 初始化连接数。应用启动时，Druid 会立即创建 5 个数据库连接放入连接池。这样应用一启动就有现成的连接可用，不需要等第一个请求来了才创建连接（创建连接是耗时操作，通常需要几百毫秒）。

min-idle: 5 —— 最小空闲连接数。连接池会始终保持至少 5 个空闲连接。如果因为超时或其他原因导致空闲连接数低于 5，Druid 会自动创建新连接补充。这保证了应用随时有足够的连接应对请求。

max-active: 20 —— 最大活跃连接数。连接池中同时最多存在 20 个连接（包括空闲和正在使用的）。当 20 个连接全部被占用时，新的请求会进入等待队列，等待时间不超过 max-wait（配置的 60000 毫秒即 60 秒）。如果 60 秒内仍没有空闲连接，会抛出异常。

配合 max-wait: 60000 使用，形成完整的连接获取策略：先尝试获取空闲连接 → 没有空闲连接且总数未达上限则创建新连接 → 达到上限则等待最多 60 秒 → 超时则抛异常。


═══════════════════════════════════════════════════════
八、项目改进与扩展
═══════════════════════════════════════════════════════

Q26：如果让你重新设计这个项目，你会做哪些改进？
考察点：反思能力、技术视野

标准回答：
如果重新设计，我会在以下几个方面改进：

架构层面：
① 引入统一的响应封装类 Result<T>，包含 code、message、data 三个字段，替代当前直接返回 boolean 或实体的方式，让前端能统一处理成功和失败情况。
② 引入全局异常处理器 @ControllerAdvice，统一捕获和处理异常，避免返回 500 错误页面。
③ 引入 Swagger/SpringDoc 自动生成 API 文档，方便前后端联调。

安全层面：
① 使用 Spring Security + JWT 实现认证和授权。
② 密码使用 BCrypt 加密存储。
③ 引入操作日志审计。

数据库层面：
① 添加数据库索引优化查询性能。
② 使用 Flyway 或 Liquibase 管理数据库版本迁移。
③ 对敏感字段（如密码、手机号）加密存储。

前端层面：
① 引入 axios 替代原生 fetch，统一处理请求拦截（添加 Token）和响应拦截（错误处理）。
② 引入 Vue Router 和 Pinia，为后续功能扩展打好基础。
③ 引入 Element Plus 或 Ant Design Vue 组件库，提升 UI 一致性和开发效率。

工程化层面：
① 使用 Docker 容器化部署，简化环境配置。
② 引入 CI/CD（GitHub Actions），代码推送后自动构建和部署。
③ 补充单元测试和集成测试，提高代码质量。

───────────────────────────────────────────────────────

Q27：现在每个 Controller 都有重复的 CRUD 代码，如何优化？
考察点：代码复用意识

标准回答：
确实，当前五个 Controller 的代码结构高度相似，都有 getAll、getById、save、update、delete、分页查询等方法，存在大量重复代码。

优化方案一：抽取 BaseController 泛型基类。
创建一个抽象的 BaseController<S extends IService<T>, T>，在基类中注入 Service，实现通用的 CRUD 方法。子类只需要继承 BaseController 并指定泛型参数，不需要重复写任何方法。这样五个 Controller 可以缩减到每个只有一两行代码。

优化方案二：利用 MyBatis-Plus 的通用能力。
MyBatis-Plus 的 IService 已经封装了所有通用 CRUD 方法，理论上可以直接写一个通用的 GenericController，通过泛型参数动态指定实体类型和 Service 类型。但这种方案路由不好统一设计，实际中用得较少。

优化方案三：代码生成器。
MyBatis-Plus 自带代码生成器，可以自动生成 Controller、Service、Mapper 代码。对于标准的 CRUD 接口，生成后不需要修改就能直接用。

但我认为适度的重复是可以接受的。如果将来某个模块需要添加特殊的业务逻辑（比如删除船舶前要检查是否有关联的航次和任职记录），独立的 Controller 反而更容易扩展。关键是在"复用"和"灵活"之间找到平衡。

───────────────────────────────────────────────────────

Q28：前端目前用的是原生 fetch，如果改用 axios，有什么好处？
考察点：HTTP 客户端的理解

标准回答：
当前使用原生 fetch 发送请求，每个请求都需要手动处理一些通用逻辑。改用 axios 有以下好处：

第一，请求拦截器。可以在请求发出前统一添加公共逻辑，比如在 Header 中注入 JWT Token、设置 Content-Type。不需要每个 fetch 调用都手动加 headers。
axios.interceptors.request.use(config => { config.headers.Authorization = 'Bearer ' + token; return config; })

第二，响应拦截器。可以统一处理响应和错误。比如所有 401 响应自动跳转登录页，所有网络错误统一弹出提示。用 fetch 的话，每个请求都要手动检查 response.ok 和 catch 异常。
axios.interceptors.response.use(null, error => { if (error.response.status === 401) logout(); })

第三，自动 JSON 转换。axios 会自动将响应体解析为 JSON，不需要每次调用 response.json()。fetch 需要手动两步：const res = await fetch(); const data = await res.json()。

第四，超时控制。axios 可以设置全局或单个请求的超时时间（timeout: 5000），超时自动报错。fetch 需要用 AbortController 手动实现，代码更复杂。

第五，请求取消。axios 支持通过 CancelToken 取消未完成的请求，比如用户快速切换 Tab 时取消上一次未完成的请求，避免数据错乱。

───────────────────────────────────────────────────────

Q29：你的系统目前没有日志功能，如何添加？
考察点：运维意识

标准回答：
日志系统分后端日志和前端日志两部分：

后端日志：
Spring Boot 默认集成了 Logback 日志框架（通过 SLF4J 门面），只需要在 application.yaml 中配置日志级别和输出路径即可。
① 在 Service 层注入 Logger，记录关键业务操作：logger.info("新增船舶: {}", shipInfo.getShipName())。
② 配置日志文件滚动策略（按天/按大小分割），避免日志文件无限增长。
③ 不同环境使用不同配置：开发环境日志级别 DEBUG 输出到控制台，生产环境 INFO 级别输出到文件。

进阶做法是引入 AOP 切面自动记录接口调用日志：
① 定义一个 @Log 注解。
② 创建 AOP 切面，拦截所有标注了 @Log 的方法。
③ 在切面中记录方法名、入参、返回值、执行耗时。
④ 将日志异步写入数据库或文件，方便查询和分析。

前端日志：
① 关键操作（登录、退出、数据增删改）发送日志到后端日志接口。
② 引入前端埋点 SDK，记录用户行为（页面访问、点击事件、错误上报）。
③ 使用 window.onerror 或 Vue 的 errorHandler 全局捕获前端异常，上报到后端。

生产环境还可以引入 ELK（Elasticsearch + Logstash + Kibana）日志分析平台，集中管理和检索日志。


═══════════════════════════════════════════════════════
九、综合场景题
═══════════════════════════════════════════════════════

Q30：用户反馈"船舶信息页面加载很慢"，你会从哪些方面排查？
考察点：全链路排查能力

标准回答：
我会按照"前端 → 网络 → 后端 → 数据库"的顺序逐层排查：

第一步，前端排查：
① 打开浏览器开发者工具 Network 面板，查看接口响应时间。如果 TTFB（首字节时间）很长，说明问题在后端；如果 TTFB 正常但页面渲染慢，说明问题在前端。
② 检查是否一次性加载了太多数据。当前分页每页 10 条，应该不会有渲染问题。
③ 检查是否有不必要的重渲染。Vue DevTools 可以查看组件渲染次数。

第二步，网络排查：
① 检查请求和响应的数据量。如果单条记录字段很多（ship_info 有 20 多个字段），响应体可能较大。
② 检查是否启用了 Gzip 压缩。生产环境 Nginx 已配置 Gzip，开发环境没有。
③ 如果是生产环境，检查 CDN 是否正常、DNS 解析是否慢。

第三步，后端排查：
① 查看后端日志，确认接口处理耗时。
② 检查是否有慢 SQL。Druid 的 StatFilter 可以看到每条 SQL 的执行时间。
③ 检查 JVM 内存和 GC 情况，是否有频繁 Full GC 导致停顿。

第四步，数据库排查：
① 直接在 MySQL 中执行分页 SQL，看执行时间。
② 用 EXPLAIN 分析 SQL 执行计划，检查是否走了索引、是否有全表扫描。
③ 检查表数据量。如果数据量很大，考虑加索引或优化查询。
④ 检查是否有锁等待：SHOW PROCESSLIST 查看是否有长时间锁定的查询。

通过这种逐层排查的方式，可以快速定位问题出在哪一层，然后针对性优化。


═══════════════════════════════════════════════════════
十、部署与运维（阿里云轻量服务器）
═══════════════════════════════════════════════════════

Q31：你的项目是怎么部署的？请描述整个部署架构。
考察点：部署能力、全栈思维

标准回答：
项目部署在阿里云轻量应用服务器上，操作系统是 Ubuntu，公网 IP 是 8.134.113.34。整体架构如下：

前端部署：
① 本地执行 npm run build，Vite 将 Vue 项目打包成静态文件（HTML、CSS、JS、图片等），输出到 dist 目录。
② 将 dist 目录的内容上传到服务器的 /www/wwwroot/ship-front 目录。
③ Nginx 配置 server 块监听 80 端口，root 指向该目录，直接返回静态文件。

后端部署：
① 本地通过 Maven 打包（mvnw.cmd package -DskipTests），生成 JAR 文件。
② 将 JAR 文件上传到服务器。
③ 使用 nohup java -jar xxx.jar & 命令在后台启动 Spring Boot 应用，监听 localhost:2005。
④ 所有 API 路径以 /api 为前缀（server.servlet.context-path=/api）。

Nginx 反向代理：
Nginx 作为统一入口，监听 80 端口。配置了 location /api/ 块，将请求 proxy_pass 到 http://127.0.0.1:2005/api/。这样前端发出的 /api/shipInfo/list 请求，Nginx 会转发给后端的 Spring Boot 处理。

此外还配置了：
① Gzip 压缩：压缩 HTML、CSS、JS、JSON 等文本类型的响应，减少传输体积。
② SSL/HTTPS：配置了 SSL 证书，支持 TLSv1 到 TLSv1.3 协议，保证数据传输安全。

用户访问流程：浏览器 → 阿里云公网 IP:80 → Nginx → 静态文件直接返回 / API 请求转发到 Java 后端 → 后端查询 MySQL 返回数据。

───────────────────────────────────────────────────────

Q32：为什么选择阿里云轻量应用服务器而不是 ECS？
考察点：云服务选型理解

标准回答：
选择轻量应用服务器主要基于性价比和易用性的考虑：

轻量应用服务器的优势（适合我的场景）：
① 价格更低。同等配置下，轻量服务器比 ECS 便宜不少，对个人项目和毕业设计来说更经济。
② 操作简单。自带可视化的管理控制台，防火墙规则、快照、监控等都在一个页面上，不需要深入理解 VPC、安全组等复杂网络概念。
③ 自带 Workbench 网页终端。可以直接在浏览器里 SSH 登录服务器执行命令，不需要额外安装 SSH 客户端，在学校电脑上也能方便运维。
④ 固定公网 IP。轻量服务器自带公网 IP（我的 8.134.113.34），不需要额外购买弹性 IP。

ECS 的优势（我目前不需要的）：
① 网络配置更灵活。支持 VPC、安全组、负载均衡等，适合多服务器集群部署。
② 规格选择更多。从 1 核 1G 到几百核几千G 都有，适合各种规模的企业应用。
③ 可加入弹性伸缩组，实现自动扩缩容。

如果项目发展到需要多台服务器、负载均衡、或者对接其他阿里云服务（如 RDS、OSS），我会考虑迁移到 ECS。

───────────────────────────────────────────────────────

Q33：Nginx 在你的项目中起到了什么作用？反向代理的原理是什么？
考察点：Nginx 核心概念

标准回答：
Nginx 在我的项目中承担三个角色：

第一，静态资源服务器。前端 Vue 项目打包后的 HTML、CSS、JS、图片等文件放在 /www/wwwroot/ship-front 目录下。Nginx 监听 80 端口，用户访问时直接返回这些静态文件，不需要经过后端 Java 进程。Nginx 处理静态文件的效率非常高，因为它使用事件驱动模型，不创建新线程。

第二，反向代理服务器。所有 /api/ 开头的请求，Nginx 通过 proxy_pass 指令转发到 http://127.0.0.1:2005/api/，也就是本机的 Spring Boot 后端。后端处理完后返回数据，Nginx 再把响应返回给浏览器。

第三，SSL 终端。HTTPS 证书配置在 Nginx 层，Nginx 负责 SSL 加解密，后端 Java 应用仍然用 HTTP 通信（localhost 不需要加密）。

反向代理的原理：
正向代理是客户端知道代理服务器的存在，由代理代替客户端去访问目标服务器（如 VPN）。反向代理则是客户端不知道真正服务器的存在，请求发到 Nginx，由 Nginx 决定转发到哪台后端服务器。

在我的配置中，浏览器请求 http://8.134.113.34/api/shipInfo/list，Nginx 收到后根据 location /api/ 规则，将请求转发到 http://127.0.0.1:2005/api/shipInfo/list。浏览器只知道 Nginx 的地址，不知道后端 Spring Boot 的存在。

好处：① 前端和后端同域，没有跨域问题；② 后端地址变化时只需要改 Nginx 配置，前端不用改代码；③ Nginx 可以做负载均衡，将来有多台后端服务器时自动分发请求。

───────────────────────────────────────────────────────

Q34：你的 Nginx 配置了 Gzip 压缩，它的作用是什么？压缩的是什么内容？
考察点：性能优化意识

标准回答：
Gzip 是一种 HTTP 压缩算法，作用是减小服务器返回给浏览器的响应体体积，从而减少网络传输时间和带宽消耗。

压缩的内容：Nginx 配置 Gzip 后，会对文本类型的响应体进行压缩，包括 HTML、CSS、JavaScript、JSON、XML 等。这些文本文件中有大量的重复模式（如 HTML 标签、CSS 属性名、JSON 的 key），Gzip 使用 DEFLATE 算法可以高效压缩，通常能压缩 60%-90%。比如一个 500KB 的 JS 文件，压缩后可能只有 100-150KB。

不压缩的内容：图片（JPEG、PNG）、视频（MP4）、音频（MP3）等已经是压缩格式的文件，再次 Gzip 压缩效果微乎其微，反而浪费 CPU 资源。

工作流程：
① 浏览器发送请求时，在请求头中带上 Accept-Encoding: gzip，告诉服务器自己支持 Gzip 解压。
② Nginx 收到请求后，如果响应体是配置的文本类型，且大小超过阈值（通常 1KB），就对响应体进行 Gzip 压缩。
③ 压缩后的响应头带上 Content-Encoding: gzip，浏览器收到后自动解压。

对于我的项目，Gzip 压缩后前端 JS 文件和 CSS 文件的传输体积大幅减小，首次加载速度明显提升，特别是在服务器带宽有限（轻量服务器通常 3-5Mbps）的情况下效果更显著。

───────────────────────────────────────────────────────

Q35：你配置了 SSL（HTTPS），为什么需要 HTTPS？HTTP 和 HTTPS 的区别是什么？
考察点：安全传输理解

标准回答：
HTTP 和 HTTPS 的核心区别在于安全性：

HTTP（超文本传输协议）：数据以明文形式在网络上传输。如果中间有人截获数据包（如公共 WiFi 下的中间人攻击），可以直接看到所有内容，包括登录密码、用户信息等敏感数据。此外，HTTP 无法验证服务器的身份，攻击者可以伪造网站。

HTTPS（HTTP over SSL/TLS）：在 HTTP 和 TCP 之间加入了 SSL/TLS 加密层。它解决了三个问题：
① 机密性：数据在传输前加密，即使被截获也无法读取内容。
② 完整性：通过消息摘要（MAC）机制，数据在传输过程中被篡改会被检测到。
③ 身份认证：通过 SSL 证书验证服务器身份，防止钓鱼网站。

我的项目配置 HTTPS 的原因：
第一，登录接口传输用户密码。如果用 HTTP，密码在网络上明文传输，非常危险。
第二，系统管理的是船舶和船员的业务数据，属于企业敏感信息，需要加密传输。
第三，现代浏览器对 HTTP 网站会显示"不安全"警告，HTTPS 是基本的信任标识。

我的 Nginx 配置支持 TLSv1 到 TLSv1.3 协议，其中 TLSv1.3 是目前最安全的版本，握手速度也更快（只需 1-RTT）。

───────────────────────────────────────────────────────

Q36：后端 JAR 包是怎么在服务器上持续运行的？如果进程挂了怎么办？
考察点：Linux 运维基础

标准回答：
当前使用 nohup 命令让 JAR 包在后台持续运行：
nohup java -jar DigitalManagement-0.0.1-SNAPSHOT.jar > app.log 2>&1 &

这条命令的含义：
① nohup（no hang up）：让进程忽略终端关闭信号（SIGHUP），即使 SSH 断开连接，进程也不会停止。
② java -jar：运行打包好的 Spring Boot JAR 文件（内嵌了 Tomcat 和所有依赖）。
③ > app.log 2>&1：将标准输出和标准错误都重定向到 app.log 文件，方便查看日志。
④ 最后的 &：将进程放到后台运行，不占用当前终端。

当前方案的不足：如果 Java 进程因为 OOM（内存溢出）或其他异常崩溃了，nohup 不会自动重启它，服务就会中断，直到人工发现并手动重启。

改进方案一：使用 systemd 配置系统服务。
创建一个 /etc/systemd/system/ship.service 文件，配置 ExecStart 为 java -jar 命令，设置 Restart=always，这样进程挂了之后 systemd 会自动重启它。同时可以设置 WantedBy=multi-user.target 实现开机自启。配置完成后执行 systemctl enable ship && systemctl start ship 即可。

改进方案二：使用 Docker 容器化部署。
编写 Dockerfile，基于 openjdk:17-jre-slim 镜像，将 JAR 文件 COPY 进去，ENTRYPOINT 设置为 java -jar。然后 docker run -d --restart=always -p 2005:2005 ship-app。Docker 的 --restart=always 策略会在容器异常退出时自动重启。

改进方案三：使用进程管理工具如 Supervisor 或 PM2，监控进程状态，异常时自动拉起。

───────────────────────────────────────────────────────

Q37：你是怎么把代码从本地弄到服务器上的？
考察点：部署流程

标准回答：
目前我的部署流程是手动操作，分两个部分：

前端部署：
① 本地执行 npm run build，Vite 将 Vue 项目打包到 dist 目录。
② 使用 scp 命令或 FTP 工具将 dist 目录下的所有文件上传到服务器的 /www/wwwroot/ship-front 目录。
③ 因为是覆盖更新，Nginx 不需要重启，新的静态文件立即生效。

后端部署：
① 本地执行 mvnw.cmd package -DskipTests，Maven 编译打包生成 JAR 文件。
② 将 JAR 文件上传到服务器指定目录。
③ SSH 登录服务器，先 kill 掉旧的 Java 进程（kill $(ps -ef | grep java | grep DigitalManagement | awk '{print $2}')），然后重新执行 nohup java -jar 命令启动新进程。

当前流程的不足：每次更新都需要手动操作，容易出错（比如忘记重启后端、上传错文件）。

改进方案：
① 使用 Git 管理代码，服务器上 clone 仓库，每次更新只需 git pull + 重新打包。
② 编写 Shell 脚本自动化部署，一条命令完成停旧进程、上传新包、启动新进程。
③ 使用 CI/CD 工具（如 GitHub Actions），配置 workflow：代码 push 到 main 分支后，自动在 GitHub 服务器上执行打包、上传、重启操作，实现零手动部署。

───────────────────────────────────────────────────────

Q38：数据库是怎么迁移到服务器上的？
考察点：数据迁移能力

标准回答：
数据库迁移分三步：

第一步，服务器安装 MySQL。
在 Ubuntu 上通过 apt install mysql-server 安装 MySQL 8.0，然后配置字符集为 utf8mb4，设置 root 密码。

第二步，导出数据。
在本地使用 SQLyog 工具连接本地 MySQL，选择 ship 数据库，右键导出 SQL 文件。导出内容包括建表语句（DDL）和插入数据的语句（DML）。对于百万级的用户数据，导出的 SQL 文件会比较大。

第三步，导入数据。
将 SQL 文件上传到服务器，然后通过 mysql 命令行导入：
mysql -u root -p ship < data.sql
这条命令会读取 data.sql 文件中的所有 SQL 语句，在 ship 数据库中执行建表和插入数据。

导入完成后，创建应用专用的数据库用户（而不是让应用使用 root 账号），并授权：
CREATE USER 'tongyan'@'localhost' IDENTIFIED BY 'password';
GRANT ALL PRIVILEGES ON ship.* TO 'tongyan'@'localhost';

数据校验：导入完成后，通过 SQLyog 连接服务器上的 MySQL，对比本地和服务器上的表数量和数据条数，确保数据完整。

对于百万级数据，导入可能需要几分钟。如果数据量特别大，可以考虑使用 mysqldump 的 --single-transaction 参数导出（保证 InnoDB 表的一致性快照），或者直接用 MySQL 的主从复制做数据同步。

───────────────────────────────────────────────────────

Q39：如果服务器突然访问不了了，你会怎么排查？
考察点：故障排查能力

标准回答：
我会按照从外到内、从简单到复杂的顺序排查：

第一步，确认问题范围。
① 用手机和电脑分别尝试访问，排除是本地网络问题。
② 尝试 ping 8.134.113.34，看 IP 是否可达。如果 ping 不通，可能是服务器关机或网络问题。

第二步，检查阿里云控制台。
① 登录阿里云控制台，查看服务器实例状态是否正常（运行中/已停止/已过期）。
② 检查是否欠费停机。轻量服务器到期后会自动停止。
③ 查看监控面板，看 CPU、内存、磁盘是否有异常（如 CPU 100%、磁盘满）。

第三步，检查安全组和防火墙。
① 在阿里云控制台查看防火墙规则，确认 80 端口（HTTP）、443 端口（HTTPS）、22 端口（SSH）是否开放。
② SSH 登录服务器后，检查 iptables 或 ufw 防火墙规则是否误改。

第四步，检查服务进程。
SSH 登录服务器后：
① systemctl status nginx —— 检查 Nginx 是否在运行。如果没运行，执行 systemctl restart nginx。
② ps -ef | grep java —— 检查 Java 进程是否存在。如果不存在，说明后端挂了，需要重新启动。
③ netstat -tlnp | grep 80 —— 确认 80 端口是否有进程在监听。
④ netstat -tlnp | grep 2005 —— 确认后端 2005 端口是否在监听。

第五步，检查日志。
① Nginx 错误日志：cat /var/log/nginx/error.log | tail -50。
② 后端应用日志：cat app.log | tail -100，查看是否有异常堆栈。
③ 系统日志：journalctl -xe 查看系统级别的错误。

第六步，检查资源。
① df -h 查看磁盘空间是否满了（日志文件增长、临时文件堆积）。
② free -m 查看内存是否耗尽。
③ top 查看 CPU 和内存占用最高的进程。

───────────────────────────────────────────────────────

Q40：你的项目目前是单机部署，如果要实现高可用，你会怎么做？
考察点：架构扩展思维

标准回答：
单机部署的风险在于任何组件故障都会导致整个系统不可用。实现高可用需要从每个环节入手：

后端高可用：
① 部署多个 Java 实例。在两台或更多服务器上分别运行 Spring Boot JAR，或者在同一台服务器上用不同端口运行多个实例（如 2005、2006）。
② 前面加负载均衡。Nginx 配置 upstream 块，将请求轮询分发到多个后端实例。如果某个实例挂了，Nginx 的健康检查会自动将它从转发列表中移除。
③ 或者直接使用阿里云的 SLB（Server Load Balancer）服务，配置更简单，自带健康检查和故障转移。

数据库高可用：
① MySQL 主从复制。主库负责写操作，从库负责读操作，读写分离。主库故障时可以手动或自动切换到从库。
② 使用阿里云 RDS（关系型数据库服务）替代自建 MySQL，RDS 自带主从切换、自动备份、监控告警等高可用能力。
③ 定期备份数据到 OSS 对象存储，防止数据丢失。

前端/静态资源高可用：
① 将前端打包后的静态文件上传到阿里云 OSS，配合 CDN 分发。即使服务器挂了，静态资源仍然可以通过 CDN 访问。
② 或者使用多台 Nginx 服务器，通过 DNS 轮询分发。

会话/状态管理：
① 引入 Redis 集群存储用户 Token 和 Session。多台后端实例共享同一个 Redis，避免单机 Session 丢失导致用户被强制退出。
② 如果使用 JWT（无状态 Token），则不需要集中存储 Session，天然支持多实例部署。

监控告警：
① 使用阿里云云监控或 Prometheus + Grafana 监控服务器 CPU、内存、磁盘、网络等指标。
② 配置告警规则，当指标异常时通过短信、钉钉、邮件等方式通知运维人员。
③ 使用健康检查接口（如 Spring Boot Actuator 的 /actuator/health）自动检测应用状态。

整体架构演进：单机 Nginx + 单机 Java + 单机 MySQL → 负载均衡 + 多实例 Java + Redis + MySQL 主从 → 容器化（Docker + K8s）+ 微服务。根据业务规模逐步演进，不过度设计。

💡 面试建议：以上 40 个问题覆盖了项目概述、技术选型、数据库设计、前后端实现、测试、安全、性能优化、部署运维等维度。建议重点准备 Q7-Q14（后端核心实现）、Q17-Q19（前端核心实现）和 Q31-Q35（部署相关），这些是面试官最可能深挖的区域。
